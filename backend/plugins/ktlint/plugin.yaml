tasks:
  lint:
    action: !shop.voenix.ktlint.runKtlint
      sources: ${module.kotlinJavaSources}
      additionalSourceRoots:
        - ${module.rootDir}/test
        - ${project.rootDir}/plugins

checks:
  - name: ktlint
    performedBy: lint

commands:
  - name: ktlint
    performedBy: lint
